import * as React from 'react';
import { configure, addDecorator, addParameters } from '@storybook/react';

export default {
  title: 'Badge',
  component: <span>saa</span>,
};

export const badge = () => <span>saa</span>;
